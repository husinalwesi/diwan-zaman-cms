import { ViewImageDirective } from './view-image.directive';

describe('ViewImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewImageDirective();
    expect(directive).toBeTruthy();
  });
});
