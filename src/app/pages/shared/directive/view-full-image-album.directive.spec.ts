import { ViewFullImageAlbumDirective } from './view-full-image-album.directive';

describe('ViewFullImageAlbumDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewFullImageAlbumDirective();
    expect(directive).toBeTruthy();
  });
});
