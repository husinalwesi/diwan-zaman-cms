import { RemoveDashsPipe } from './remove-dashs.pipe';

describe('RemoveDashsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveDashsPipe();
    expect(pipe).toBeTruthy();
  });
});
